Read `docs/orchestrator-instructions.md` and follow it for this request.

The task and role settings from this invocation are:

$ARGUMENTS

On the init dispatch call, pass the active shell's `CODEX_THREAD_ID` value as
`--parent-session`. Use the active shell's environment-variable syntax. Do not
print the value.
